// just useful constants to share between classes
#ifndef STATE_H_INCLUDED
#define STATE_H_INCLUDED

enum State {MINE=-1, COVERED, UNCOVERED};
enum Flag {UNFLAGGED = false, FLAGGED = true};

#endif // STATE_H_INCLUDED
